# API base URL for production builds (Vercel).
# Local dev uses package.json "proxy" — leave unset or use /api for CRA proxy.
#
# Production example (Railway backend):
# REACT_APP_API_URL=https://your-service.up.railway.app/api
#
REACT_APP_API_URL=
